// components/AdminUI.tsx
"use client";

import FormModule, { FormField, FormRow, FormActions, SubmitButton, SearchBar } from "./AdminForm";

const AdminUI = {
  FormField,
  FormRow,
  FormActions,
  SubmitButton,
  SearchBar,
  defaultForm: FormModule,
};

export default AdminUI;
